#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a: first integer to multiply
 * @b: second integer to multiply with the first number
 * Return: 0 (success)
 */

int mul(int a, int b)
{
	return (a * b);
}
